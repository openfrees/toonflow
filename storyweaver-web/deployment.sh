#!/bin/bash

echo '========================================='
echo '  知剧AI前端部署脚本'
echo '========================================='

# 1. 覆盖服务器修改，强制与远程同步
echo '步骤 1/3: 跳过拉取代码的步骤...'
# git reset --hard
# git pull origin main

# 2. 可选：重新安装依赖（传入 new 参数时执行）
if [ "$1" == 'new' ]; then
    echo '步骤 2/3: 重新安装依赖...'
    npm install
else
    echo '步骤 2/3: 跳过依赖安装（如需重装请传入 new 参数）'
fi

# 3. 编译生成静态站点
echo '步骤 3/3: 编译生成静态站点...'
npm run generate:prod

echo '========================================='
echo '  部署完成！'
echo '  静态文件位置: .output/public/'
echo '========================================='
